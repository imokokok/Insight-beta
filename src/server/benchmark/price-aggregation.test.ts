import { it, describe } from 'vitest';

/**
 * 价格聚合基准测试套件
 *
 * 注意：这些测试被跳过，因为它们需要 benchmark 模式运行
 * 使用 `npm run test:bench` 运行基准测试
 */

describe('Price Aggregation Benchmarks', () => {
  it.skip('simple average aggregation', () => {
    // Benchmark: simple average aggregation
  });

  it.skip('weighted average aggregation', () => {
    // Benchmark: weighted average aggregation
  });

  it.skip('median calculation', () => {
    // Benchmark: median calculation
  });

  it.skip('outlier detection with IQR', () => {
    // Benchmark: outlier detection with IQR
  });

  it.skip('outlier detection with Z-score', () => {
    // Benchmark: outlier detection with Z-score
  });

  it.skip('complete aggregation pipeline', () => {
    // Benchmark: complete aggregation pipeline
  });
});

describe('Protocol Data Fetching Benchmarks', () => {
  it.skip('parse 1000 price updates', () => {
    // Benchmark: parse 1000 price updates
  });

  it.skip('calculate price deviation', () => {
    // Benchmark: calculate price deviation
  });
});
