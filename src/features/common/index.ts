/**
 * Common Feature Module - 通用功能模块
 *
 * 存放跨多个功能域共用的组件、hooks、服务和类型。
 *
 * 目录结构：
 * common/
 * ├── components/   # 通用组件
 * ├── hooks/       # 通用 hooks
 * ├── services/    # 通用服务
 * ├── types/       # 通用类型
 * └── index.ts     # 统一导出
 */

export {};

/**
 * 通用组件导出示例：
 * export { CommonCard } from './components/CommonCard';
 *
 * 通用 Hooks 导出示例：
 * export { useCommonFunction } from './hooks/useCommonFunction';
 *
 * 通用服务导出示例：
 * export { CommonService } from './services/commonService';
 *
 * 通用类型导出示例：
 * export type { CommonType } from './types/commonTypes';
 */
